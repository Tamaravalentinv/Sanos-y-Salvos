spring.datasource.url=${NOTIFICACIONES_DB_URL}
spring.datasource.username=${NOTIFICACIONES_DB_USER}
spring.datasource.password=${NOTIFICACIONES_DB_PASSWORD}

# Disable mail health check (SMTP not configured in Docker)
management.health.mail.enabled=false
